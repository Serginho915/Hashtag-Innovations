export interface TextbookItem {
  id?: string;
  title: string;
  excerpt: string;
  authorLabel: string;
  authorName: string;
  authorExpertId?: string;
  imageUrl: string;
  pdfUrl: string;
  createdAt?: string;
}
